'use strict';

describe('Factory: AWSFactory',function(){

  beforeEach(module('jwwebApp'));
  var AWSFactory, scope;

  beforeEach(inject(function(AWSFactory){
    aws = AWSFactory;
  }))

  

  it('should have multipart form data',function(){
    expect();
  });

});